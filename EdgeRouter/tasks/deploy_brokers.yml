---
- name: Start Broker A
  docker_container:
    name: Broker.A
    image: vromero/activemq-artemis
    state: started
    networks:
      - name: lab0
        aliases:
          - BA
        links:
          - I1
    volumes:
      - "{{playbook_dir}}/generated/Broker.A/etc:/var/lib/artemis/etc"
